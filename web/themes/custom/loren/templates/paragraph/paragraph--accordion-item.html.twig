
{%
  set classes = [
    'section',
    'section--type-' ~ paragraph.bundle|clean_class,
    view_mode ? 'section--view-mode-' ~ view_mode|clean_class,
  ]
%}

<div{{ attributes.addClass(classes) }}>
  {# add wrapper and classes #}
    {{ content.field_section_title }}
    <div class="accordion-content js-to_expand">
      {{ content.field_section_body }}
    </div>

</div>
